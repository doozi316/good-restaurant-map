<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.map.restaurant.good.dao.TestDAO">
    <select id="getTestData" resultType="com.map.restaurant.good.dto.TestDTO">
        select
            title,
            name
        from
            DB_TEST
    </select>
</mapper>
